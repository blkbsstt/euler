package euler

object Problem056 extends App {
    /*
    def lychrel(n: Int): Boolean = {
        def helper(i: Int, n: BigInt): Boolean = {
            val m = n + n.reversed
            i <= 50 && (m.palindromic || helper(i + 1, m))
        }
        helper(1, n)
    }

    println((1 to 9999).filter(lychrel).size)
    */
}
